<% if (post.categories && post.categories.length){ %>
  <div class="category">
    <%- list_categories(post.categories, {
      show_count: false,
      class: '',
      style: 'none',
      separator: '',
      transform: function(t) {
        return __(t);
      }
    }) %>
  </div>
<% } %>
