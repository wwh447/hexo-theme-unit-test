<% if (post.photos && post.photos.length){ %>
<div class="gallery">
  <% post.photos.forEach(function(photo, i){ %>
    <a class="swiper-slide" href="<%- url_for(photo) %>">
      <img src="<%- url_for(photo) %>" itemprop="image">
    </a>
  <% }) %>
</div>
<% } %>
